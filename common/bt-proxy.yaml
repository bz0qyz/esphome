bluetooth_proxy:
  active: True
  connection_slots: ${connection_slots}

esp32_ble:
  max_connections: ${max_connections}

esp32_ble_tracker:
  scan_parameters:
    continuous: ${continuous}
    active: ${active}